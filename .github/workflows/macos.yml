name: macOS

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - name: cmake
      run: cmake -S . -B build
    - name: build
      run: cmake --build build
    - name: test
      run: cd build ; ctest -j 10
